#!/usr/bin/env Rscript

args <- commandArgs(trailingOnly = TRUE)
message("run_seurat_basic.R is a guarded placeholder.")
message("Install R >= 4.3, Seurat, and use workspace/inputs before enabling local execution.")
quit(status = 1)

